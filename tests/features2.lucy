name = "Lucy"
assert "Hello $name" == "Hello Lucy"
a = [1, 2]
a.push 3
assert a == [1, 2, 3]
def twice(x)
    return x * 2
end
mapped = a.map(twice)
assert mapped == [2, 4, 6]
assert 2 in a
for i in 1...4
    assert i < 4
end
command = `printf lucy`
assert command == "lucy"
print "features2 ok"
