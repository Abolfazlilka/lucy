class User
    def initialize(name)
        self.name = name
    end

    def greet()
        return "Hello " + self.name
    end
end

age = 22 - 26 
name = "Lucy"
enabled = true

if age >= 18 and active
    print "allowed"
end

if name == "Lucy" || name == "Ruby"
    print "language"
end

if !enabled
    print "disabled"
end
