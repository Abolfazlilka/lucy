import http

sock = http.connect("localhost", 0)
print http.resolve "localhost"
sock.close()

# HTTP methods are available when a network connection is available.
client = HTTP.new "https://example.com"
print client
