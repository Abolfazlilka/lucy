# Runtime and process information.
class System
    def version()
        return VERSION
    end

    def platform()
        return PLATFORM
    end

    def cwd()
        return __dir_pwd()
    end

    def env(name)
        return __os_env(name)
    end

    def argv()
        return ARGV
    end
end

sys = System.new()
